version: 2

sources:
  - name: raw
    schema: gz_raw_data
    tables:
      - name: sales
        schema: raw_gz_sales
        tables:
      - name: product
        schema: raw_gz_product
        tables: 
      - name: ship
        schema: raw_gz_ship
        tables: 


